#include "main.h"

/**
 * main - print alphabet ten times followed ny new line
 *
 * Return: 0
 */
void print_alphabet_x10(void)
{
	char h, b;
	for (b = 0; b<= 9; b++);

	{
		for (h = 'a' ; h <= 'z'; h++);

		{
			_putchar(h);
		}
		_putchar('\n');
	}
}
